pluginGroup = com.github.fprime_community.fpp_tools
pluginName = FPP
pluginRepositoryUrl = https://github.com/fprime-community/fpp-tools
# SemVer format -> https://semver.org
pluginVersion = 1.0.0

# Supported build number ranges and IntelliJ Platform versions -> https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html
pluginSinceBuild = 252

# IntelliJ Platform Properties -> https://plugins.jetbrains.com/docs/intellij/tools-gradle-intellij-plugin.html#configuration-intellij-extension
# Platform type/version used when no local IDE is available (e.g. in CI).
# CL = CLion. Pin to the latest stable release (not an EAP snapshot) so the
# download is deterministic and cacheable.
# See https://plugins.jetbrains.com/docs/intellij/tools-intellij-platform-gradle-plugin-types.html
platformType = CL
platformVersion = 2026.2.0.1

# Optionally build against a locally-installed IDE (faster dev builds) instead of
# downloading the platform defined above. Do NOT commit a machine-specific path
# here. Set it per-developer via either:
#   - the LOCAL_IDE_PATH environment variable, or
#   - `localIdePath=/path/to/IDE.app` in your personal ~/.gradle/gradle.properties
# If neither is set (e.g. in CI), the platform above is downloaded automatically.

# Plugin Dependencies -> https://plugins.jetbrains.com/docs/intellij/plugin-dependencies.html
# Example: platformPlugins = com.jetbrains.php:203.4449.22, org.intellij.scala:2023.3.27@EAP
platformPlugins =
# Example: platformBundledPlugins = com.intellij.java
platformBundledPlugins =
# Example: platformBundledModules = intellij.spellchecker
platformBundledModules =

# Gradle Releases -> https://github.com/gradle/gradle/releases
gradleVersion = 9.3.1

# Opt-out flag for bundling Kotlin standard library -> https://jb.gg/intellij-platform-kotlin-stdlib
kotlin.stdlib.default.dependency = false

# Enable Gradle Configuration Cache -> https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache = true

# Enable Gradle Build Cache -> https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching = true
